# libs

Self-contained libraries providing reusable functionality independent of the application (e.g. a message parser). Each library has a versioned API and may be distributed as a separate package or repository.

These do **not** depend on components.